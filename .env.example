DB_ROOT_PASSWORD=password-root
DB_NAME=ecommerce_hoanthom
DB_USER=ecommerce_app
DB_PASSWORD=password

# Domain miễn phí trỏ thẳng vào IP server (không cần mua domain) — thay đúng IP của bạn.
# Caddy tự xin chứng chỉ HTTPS thật (Let's Encrypt) cho domain này, không cần cấu hình thêm.
# Nếu sau này mua domain thật, chỉ cần đổi giá trị này.
SITE_ADDRESS=139.180.131.46.nip.io
